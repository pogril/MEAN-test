const path = require('path');

exports.getTickets = (req, res, next) => {

    //get relevent tickets according to user role, sort by priority

    res.json({});
}

exports.getOne = (req, res, next) => {

    //fetch one ticket by _id

    res.json({});
}

exports.getMany = (req, res, next) => {

    //get all tickets that match conditions passed in through req.body
    
    //customizable output sorting

    res.json({});
}